# Black Box Observation Prompt Template

Use `policy_interp.audit_evidence_suite.render_blackbox_generator_prompt(package)` to render a text only observation prompt. The generator must not receive hidden state, feature, logit, steering, oracle, proxy, or retrieval labels.
